git config –-global user.name "Emma-Rsn"
git config –-global user.name
git config –-global user.email "rasson.emma@gmail.com"
git config –-global user.email

mkdir workspace
git init
git status

touch .gitignore
nano .gitignore
git config --global core.excludesfile ./.gitignore
git add .gitignore
git commit -m 'gitignore'

nano fich1.c
git add fich1.c
git commit -m 'ajout fich1'

nano fich2.c
nano fich3.c
git add fich2.c fich3.c
git commit -m 'ajout fich2 et fich3' -> -m permet d'ajouter un label à la commande commit
git status
git log

